Dynamical systems: how initial conditions affect the final state of a deterministic system

Libration mode (stays within $\left| \pi \right|$ and rotation mode allows $> \left| \pi \right|$. Stays in these modes depending on initial conditions unless damping occurs allowing transition from rotation to transition.

> [!definition|*]- Non-linearity
> A system non-linear if the dependent variable appears as a power or non-linear function. Almost certainly no closed form solution. Potentially multiple end states
 ^def-de-a-non-lin

> [!definition|*]- Dimension
>  Given an $n$th ordered system, its dimension is $n$. We may write the $n$th order ODE as $\underline{\dot{x}}=\underline{\gamma}(\underline{x})$
 ^def-de-dim

$\ddot{\theta}+\varepsilon \dot{\theta}+\sin \theta=0$
$\dot{\theta}=x$
$\dot{x}=-\varepsilon x-\sin \theta$
$\dot{\underline{y}}=\begin{pmatrix}\dot{\theta} \\ \dot{x}\end{pmatrix}=\underline{\gamma}(\underline{y})\qquad y=\begin{pmatrix}\theta \\ x\end{pmatrix}$

> [!definition|*]- Autonomous
> A system is autonomous if there is no explicit time dependence in the equation 
 ^def-DE-autonomous
 